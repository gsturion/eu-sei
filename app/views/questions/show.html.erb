<nav class="navbar fixed-top navbar-expand-sm navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-collapse">☰</button>
  <div class="collapse navbar-collapse" id="navbar-collapse">
    <ul class="nav navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Eu sei!</a>
        <div class="dropdown-menu dropdown-menu-right">
          <% if current_user %>
            <%= link_to "Perfil", profile_path, class: "dropdown-item" %>
              <% if current_user.is_teacher %>
                <%= link_to "Salas de Aula", classrooms_path, class: "dropdown-item" %>
              <% else %>
                <%= link_to "Sala de aula", feed_path(@classroom), class: "dropdown-item" %>
              <% end %>

            <%= link_to "Sair", destroy_user_session_path, class: "dropdown-item", method: :delete%>
          <% else %>
            <%= link_to "Login", user_session_path, class: "dropdown-item" %>
            <%= link_to "Cadastre-se", new_user_registration_path, class: "dropdown-item" %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>

<div class="container mt-5 pt-5">
  <div class="row justify-content-center">
    <div class="col-xs-12 col-sm-4 whitebox">
      <h4 class="text-center mt-2 mb-5">Questão criada com sucesso!</h4>
      <p>Questão:</p>
      <div>
        <div class="d-flex justify-content-between pt-3"><%= @question.content %>
        <div class="d-flex justify-content-between">
          <%= link_to "", edit_question_path(@question), method: :get , class: "far fa-edit  text-decoration-none pr-3" %>
          </div>
        </div>
      </div>
      <p>Alternativas:</p>
        <% @question.alternatives.each do |alternative| %></p>
          <div class="d-flex justify-content-between pt-3 text-decoration-none"><%= "#{alternative.content}" %>
            <div class="d-flex justify-content-between">
          <%= link_to "", edit_alternative_path(alternative), method: :get , class: "far fa-edit  text-decoration-none pr-3" %>
          </div>
          </div>
        <% end %>

        <div class=" btn btn-flat btn-block mt-5 mb-4"><%= link_to 'Questões', questions_path, class: "text-white" %></div>
    </div>
  </div>
</div>

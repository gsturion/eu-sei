<%= simple_form_for [@classroom, @question] do |f| %>
  <%= f.input :lesson %>
  <%= f.input :content %>
  <%= f.input :released_at %>
  <%= f.submit "Criar questão", class: "btn btn-primary" %>
<% end %>
